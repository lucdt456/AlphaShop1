@model IEnumerable<AlphaShop1.Models.BrandModel>

@{
	ViewData["Title"] = "Brand";
	Paginate pager = new Paginate();
	int pageNo = 0;
	if (ViewBag.Pager != null)
	{
		pager = ViewBag.Pager;
		pageNo = pager.CurrentPage;
	}
}

<div class="custom-container">
	<div class="row">
		<div class="col-6">
			<h3>List Brands</h3>
		</div>
		<div class="col-6 text-right">
			<a asp-action="Create" asp-controller="Brand" class="btn btn-primary"><i class="fa-solid fa-plus"></i><strong> Add Brand</strong></a>
		</div>
	</div>

	<br />
	<table class="table table-light table-borderless table-hover text-center mb-0">
		<thead class="thead-dark">
			<tr>
				<th>#</th>
				<th>Name</th>
				<th>Description</th>
				<th>Slug</th>
				<th>Status</th>
				<th></th>
			</tr>
		</thead>
		<tbody class="align-middle">
			@foreach (var item in Model)
			{
				<tr>
					<td class="align-middle">@item.Id</td>
					<td class="align-middle">@item.Name</td>
					<td class="align-middle">@item.Description</td>
					<td class="align-middle">@item.Slug</td>

					@if (item.Status == 1)
					{
						<td class="align-middle">Hiển thị</td>
					}
					else
					{
						<td class="align-middle">Ẩn</td>
					}

					<td class="align-middle">
						<a asp-action="Edit" asp-controller="Brand" asp-route-Id="@item.Id" class="btn btn-sm btn-primary"><i class="fa-solid fa-pencil"></i></a>
						<a asp-action="Delete" asp-controller="Brand" asp-route-Id="@item.Id" class="btn btn-sm btn-danger ml-1"><i class="fa-solid fa-x"></i></a>
					</td>
				</tr>
			}
		</tbody>
	</table>
	<partial name="_PVPhanTrang" model="@pager" />
</div>

<style>
	.custom-container {
		max-width: 90%;
		margin: 50px auto;
	}
</style>